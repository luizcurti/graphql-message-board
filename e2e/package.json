{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "description": "GraphQL Message Board — browser E2E tests driving the real front-end against the real back-end (docker compose).",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
